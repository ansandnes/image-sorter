# Test fixtures

`base.jpg` and `base.mov` are sample files from the ExifTool test suite
(`t/images/Writer.jpg` and `t/images/QuickTime.mov`, https://exiftool.org, same license as Perl).
The end-to-end test copies them and writes dates and GPS positions into the copies with ExifTool.
